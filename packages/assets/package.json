{
  "name": "@body-diary/assets",
  "version": "0.0.1",
  "private": true,
  "description": "Body Diary shared SVG assets — pose sketches, UI icons, body-figure layout data",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "react": ">=18"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "react": "^18.3.1"
  }
}

